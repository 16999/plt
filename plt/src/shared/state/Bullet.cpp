#include "Bullet.h"
using namespace state;



Bullet::Bullet()
{
  this->next = NULL;
}

/*
void Bullet::update(float dx, float dy, float dPhi, Map map)
{
  this->x += dx;
  this->y += dy;
  this->phi += dPhi;
}
*/
